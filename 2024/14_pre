
//== ==============================Higher Order function==============================
Higher Order function :-

A higher order function is a function that takes one or more functions as arguments, or returns a function as its result.

A function which accepts another function as an argument is called a higher
order function.
The higher order function is used to perform different operations.

Example: addEventListener

Other Common Higher-Order Functions:
map()
filter()
reduce()
forEach()
setTimeout() and setInterval()



For an interview, keep it short, accurate, and professional.

### 1. What is synchronous execution in JavaScript?

Answer:

> Synchronous execution means JavaScript executes code line by line, one statement at a time. Each task must finish before the next task starts.

Example:

```javascript
console.log("A");
console.log("B");
console.log("C");
```

Output:

```
A
B
C
```

---

### 2. What is asynchronous execution in JavaScript?

Answer:

> Asynchronous execution allows JavaScript to start a task and continue executing other code without waiting for that task to complete. When the task finishes, its callback is executed later.

Example:

```javascript
console.log("A");

setTimeout(() => {
    console.log("B");
}, 1000);

console.log("C");
```

Output:

```
A
C
B
```

---

### 3. Is JavaScript synchronous or asynchronous?

Answer:

> JavaScript is single-threaded and synchronous by default. However, it can perform asynchronous operations with the help of the runtime environment (Browser APIs or Node.js APIs) and the Event Loop.

---

### 4. How can JavaScript perform asynchronous operations if it is single-threaded?

Answer:

> JavaScript has only one call stack, but asynchronous tasks such as timers, API calls, and file operations are handled by the browser or Node.js runtime. Once the task is completed, the Event Loop pushes the callback into the call stack for execution.

---

### 5. What is a callback?

Answer:

> A callback is a function passed as an argument to another function and executed later, usually after an asynchronous operation completes.

Example:

```javascript
function greet(callback) {
    callback();
}

greet(() => {
    console.log("Hello");
});
```

---

### 6. What are the ways to handle asynchronous operations in JavaScript?

Answer:
There are three main ways:

1. Callbacks
2. Promises
3. Async/Await

Example:

```javascript
// Callback
getData(callback);

// Promise
getData()
    .then(result => console.log(result))
    .catch(error => console.log(error));

// Async/Await
async function fetchData() {
    try {
        const result = await getData();
        console.log(result);
    } catch (error) {
        console.log(error);
    }
}
```

### One-line interview answer

> "JavaScript is a single-threaded language that executes code synchronously by default. Asynchronous operations are handled by the runtime environment, and the Event Loop ensures completed tasks are executed without blocking the main thread."



=====================






//=================== promise =============================



callback
callback is a function passed as an argument to another function,
which gets invoked after the main function completes its execution.
 
They are commonly used to handle asynchronous operations in JavaScript.

Callbacks are commonly used in programming to handle asynchronous tasks, such as fetching data from a server, reading files, or handling user interactions.



================================================================================

how handle asynchronous operations 

Handling asynchronous operations in programming involves
managing tasks that may take variable amounts of time to complete without blocking the execution of other code.

Asynchronous operations can be handled using various techniques, including callbacks, Promises, async/await, and event listeners


=============================================================================================================
lexical scope

When a function (Function B) is declared inside another function (Function A), JavaScript uses lexical scoping to determine what variables Function B has access to.
// 🌍 Global scope
let globalVar = "I am global";

function Function_A_outer() {
  // 🔹 Lexical Scope Level 1 (outer)
  let outerVar = "I am outer";

  function Function_b_inner() {
    // 🔸 Lexical Scope Level 2 (inner)
    let innerVar = "I am inner";

    //  Accessing variables through lexical scope
    console.log(globalVar); // ← from Global Scope
    console.log(outerVar);  // ← from outer()'s Lexical Scope
    console.log(innerVar);  // ← from inner()'s own scope
  }

  Function_b_inner(); // Call the inner function
}

Function_A_outer(); // Call the outer function











