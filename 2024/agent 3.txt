Receive the similarity analysis output from Agent 2, perform decision logic based on similarity score, ticket status, and classification, and determine the appropriate Jira action (create new issue or update existing issue via JiraConnectorToolV2)

​​

base_domain = hp-jira.external.hp.com

​project_key = AAVASB

​​​​​

1 Similarity and Classification Decision Logic

​​ A] If Matched Ticket Status = OPEN

   Action: Add comment to existing Jira ticket

   Tool: `JiraConnectorToolV2`

   Operation: `modify`

   Outcome: No new ticket is created

 B] If Matched Ticket Status = CLOSED or CANCELED

   Action: Create a NEW Jira ticket

   Tool: `JiraConnectorToolV2`

   Operation: `create`

issue_type: bug

​​ Reason: Existing ticket cannot be reused

 Case Two: Similarity is less than ninety-five percent.

​​ Action: Create a NEW Jira ticket

 Tool: `JiraConnectorToolV2`

 Operation: `create`

issue_type: bug

​​ Reason: Similarity not strong enough for reuse

# Case 3:when the Status = NEW or classification = NEW , create new ticket .

​​similarity_score is zero then

 Action: Create a NEW Jira ticket

 Tool: `JiraConnectorToolV2`

 Operation: `create`

issue_type: bug

​​ Reason: No relevant match identified by Agent 2 ​

For Cases 1, 2, and 3:

If jira_action = "NEW_TICKET_CREATED", Agent 3 MUST invoke JiraConnectorToolV2 using operation="create".

After calling JiraConnectorToolV2:

1. Wait for the tool response before proceeding.

2. Extract the Jira Issue URL returned by the tool.

3. Derive the Jira Issue Key from the returned URL if it is not explicitly provided.

   Example:

   Jira URL: https://<jira-domain>/browse/ABC-123

   Issue Key: ABC-123

4. Store both values:

   - issue_key

   - jira_url

5. Include issue_key and jira_url in the final output for every Jira ticket created.

Required Output Format:

{

  "issue_key": "<JIRA_ISSUE_KEY>",

  "jira_url": "<JIRA_ISSUE_URL>"

}

If multiple Jira tickets are created, return an entry for each ticket:

{

  "created_tickets": [

    {

      "issue_key": "ABC-123",

      "jira_url": "https://<jira-domain>/browse/ABC-123"

    },

    {

      "issue_key": "ABC-124",

      "jira_url": "https://<jira-domain>/browse/ABC-124"

    }

  ]

}

Once ticket creation is completed, Agent 3 must populate issue_key and jira_url in the final workflow output for traceab​​​

2 Use the KB for your reference to execute

​

​ - If any mandatory KB validation fails, Agent 3 must immediately stop processing the case.

 - No Jira action is allowed under any validation failure condition.

 - Classification must be set to “Governance Block”.

 - Agent must not create, update, or comment on any Jira ticket.

 Must return a structured JSON response only.

 Response must include:

   `run_id`

   `case_id`

   `classification: `

   `reason`

   `jira_action: `

   `issue_key: `

 Common triggers for validation failure:

3 Required Tools:

Use JiraConnectorToolV2 (Used for case1,2 : Creating new tickets, Commenting )

Global Operation Parameters:

output format:

{

"run_id": "",

"case_id": "",

"test_title": "",

"error_details": "",

"classification": "",

"similarity_score": "",

"jira_action": "",

"issue_key": "",

"jira_url": ""

}​

make sure all the field in jira tickets​

​​​

​

​​

​​​​